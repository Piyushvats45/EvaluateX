# All dependencies below are free / open-source. No paid APIs are used anywhere
# in this framework — every model, metric library, and storage engine is local
# and $0 cost.

# Core
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
Jinja2>=3.1
matplotlib>=3.7
scikit-learn>=1.3          # TF-IDF fallback similarity when torch/bert-score unavailable

# Local LLM inference (free, open-weight models — no API key, no per-token cost)
transformers>=4.38
torch>=2.1

# Evaluation metrics (free, open-source)
rouge-score>=0.1.2
bert-score>=0.3.13

# Optional: nicer progress bars
tqdm>=4.66

# Deployment: REST API + interactive demo UI (free to run and free to host
# on Render/Fly.io/HF Spaces free tiers, or Streamlit Community Cloud)
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.32

# Testing
pytest>=7.4
