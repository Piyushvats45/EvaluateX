
models:
  - name: distilgpt2-default
    backend: huggingface
    model_name: distilgpt2
    params:
      device: cpu

  - name: gpt2-base
    backend: huggingface
    model_name: gpt2
    params:
      device: cpu